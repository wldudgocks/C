//양수면 양수라고 알려주는 프로그램
#include <stdio.h>
int main(void)
{
    int number;   //int는 자료형(데이터의 형태를 나타냄) 중에서 정수형이다.

    printf("정수를 입력하시오 :");
    scanf("%d", &number);   //&는 입력 받을 변수의 주소를 나타냄

    if(number >0)
    {                             /* 양의 정수이면 "양수입니다."라는 문장이 모니터에 나옴,만약 0이거나 음의 정수라면 "양수입니다. 라는 문장이 나오지 않음" */
        printf("양수입니다.");
    }
    
    printf("입력된 값은 %d입니다.",number);  //쉼표 다음에 가져올 변수를 적어넣음

    return 0;
}
